version: '3.9'

services:

  postgres:
    image: postgres:16

    container_name: stock-postgres

    environment:
      POSTGRES_DB: stocks
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5432:5432"